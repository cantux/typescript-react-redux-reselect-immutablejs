import * as React from 'react';

interface HomeProps {}

export const Home: React.SFC<HomeProps> = (props: HomeProps) =>
  (<div>Hello world reminds me of the scene where Neo ripped the membrane of its cell.</div>);
